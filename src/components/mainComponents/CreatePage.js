import { CardText, CardTitle, Card } from 'reactstrap';

function CreatePage() {
  return (
    <Card>
      <CardTitle>Здесь будет блядская форма</CardTitle>
      <CardText />
    </Card>
  );
}
export default CreatePage;
